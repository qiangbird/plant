'use strict';

/* Directives */
/*
 * angular directive onBlur
 *
 * @description my ng-blur
 * @require jquery
 */
/*var plantDirectives =  angular.module('plantDirectives', []);

plantDirectives.directive('onBlured',['safeApply',function(safeApply) {
    return {
        restrict : 'A',
        link : function(scope, elm, attrs) {
//            elm.bind('blur', function() {
                safeApply(scope, $("#Home").click());
//            });
        }
    };
}]);*/